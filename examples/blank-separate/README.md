# Blank with default.nix

This is an example repository for
[hypered/blank](https://github.com/hypered/blank) containing a `default.nix`
file that disable the separation of the index and overview pages.
